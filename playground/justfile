pre-build:
	just -f ../justfile lezer build # 语法

build: pre-build
	pnpm run build
	gzip -k dist/**/*.js
	brotli -k dist/**/*.js

test: pre-build
	pnpm run type-check

serve: build serve-no-build

serve-no-build:
	# https://github.com/http-party/http-server
	http-server ./dist --brotli --gzip